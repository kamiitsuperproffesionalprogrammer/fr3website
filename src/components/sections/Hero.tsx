import React from "react";

export const Hero: React.FC = () => {
  return (
    <section className="text-black text-[32px] font-bold h-[531px] bg-white flex items-center justify-center">
      КАРТИНКА
    </section>
  );
};
